# 显式行尾规则
# - 文本文件默认按平台自动转换，但下面这些显式锁定为 LF，避免 Windows
#   开发者 git autocrlf 把脚本 / Dockerfile 转成 CRLF 后镜像内 shebang 失败。
* text=auto

*.sh   text eol=lf
*.py   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf
Dockerfile     text eol=lf
.dockerignore  text eol=lf
.env.example   text eol=lf
